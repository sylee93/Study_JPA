Spring:
  # 설정이 없으면 기본값이 인메모리 모드
#  datasource:
#    url: jdbc:h2:mem:test #인메모리 모드 : h2 종료해도 테스트 가능
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#   hibernate:
#     ddl-auto: create-drop
#   properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
   # org.hiernate.type: trace
